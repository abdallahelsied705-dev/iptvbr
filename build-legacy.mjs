// Deprecated compatibility file.
// Previous versions extracted a compressed source snapshot and overwrote the
// repository during deployment. That behaviour was unsafe and removed.
console.log("Legacy source extraction is disabled; using repository files as-is.");
